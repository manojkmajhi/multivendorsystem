# Legacy Modal Styles

The original modal CSS files were superseded by `product-quickview.css`.
They are retained here temporarily for reference and can be deleted after final verification.

Moved files (manually move or delete in your OS if still present in parent directory):
- product-modal.css (deprecated)
- product-modal-minimal.css (deprecated)

Safe removal checklist:
1. Search codebase for `product-modal.css` or `product-modal-minimal.css` (should appear only in legacy docs).
2. Load home, category, product details pages – verify quickview modal styling intact.
3. Remove both files.
